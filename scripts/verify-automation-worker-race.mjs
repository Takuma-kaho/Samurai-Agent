import{execFileSync}from"node:child_process";import{mkdirSync,mkdtempSync,readdirSync,rmSync,writeFileSync}from"node:fs";import path from"node:path";import process from"node:process";import{committedSourceEvidence}from"./lib/core-evidence.mjs";const root=process.cwd(),prefix=process.platform==="darwin"?`@esbuild+darwin-${process.arch==="arm64"?"arm64":"x64"}@`:`@esbuild+${process.platform}-${process.arch}@`,dir=readdirSync(path.join(root,"node_modules/.pnpm")).find(x=>x.startsWith(prefix));if(!dir)throw new Error("esbuild missing");const name=dir.slice(0,dir.lastIndexOf("@")).replace("+","/"),esbuild=path.join(root,"node_modules/.pnpm",dir,"node_modules",name,"bin/esbuild"),cache=path.join(root,"node_modules/.cache");mkdirSync(cache,{recursive:true});const temp=mkdtempSync(path.join(cache,"samurai-automation-race-")),out=path.join(temp,"verify.mjs"),sources=["packages/workspace-store/src/index.ts","scripts/fixtures/automation-worker-race.ts","scripts/verify-automation-worker-race.mjs","scripts/lib/core-evidence.mjs"];try{execFileSync(esbuild,[path.join(root,"scripts/fixtures/automation-worker-race.ts"),"--bundle","--platform=node","--format=esm","--external:better-sqlite3",`--outfile=${out}`],{cwd:root,stdio:"inherit"});const started_at=new Date().toISOString(),raw=execFileSync(process.execPath,[out],{cwd:root,encoding:"utf8"}).trim(),result=JSON.parse(raw),completed_at=new Date().toISOString(),evidence=path.join(root,"reports/core-completion/evidence");mkdirSync(evidence,{recursive:true});writeFileSync(path.join(evidence,"F02.json"),`${JSON.stringify({schema_version:1,test_id:"F02",command:"pnpm core:test:automation-race",status:"passed",...committedSourceEvidence(root,sources),started_at,completed_at,assertions:[{name:"10 workers racing 100 claims produce one winner and side effect",actual:{claims:result.successful_claims,side_effects:result.side_effects},expected:{claims:1,side_effects:1}},{name:"Heartbeat extends lease and blocks early reclaim",actual:result.heartbeat_extended&&result.early_reclaim_rejected,expected:true},{name:"Expired lease is reclaimed after Store restart",actual:result.restart_reclaim,expected:true},{name:"Retry cannot claim before due and claims at due time",actual:result.retry_before_due_rejected&&result.retry_at_due_claimed,expected:true}],result},null,2)}\n`);process.stdout.write(`${raw}\n`)}finally{rmSync(temp,{recursive:true,force:true})}
