import{execFileSync}from"node:child_process";import{mkdirSync,mkdtempSync,readdirSync,rmSync,writeFileSync}from"node:fs";import path from"node:path";import process from"node:process";import{committedSourceEvidence}from"./lib/core-evidence.mjs";const root=process.cwd(),prefix=process.platform==="darwin"?`@esbuild+darwin-${process.arch==="arm64"?"arm64":"x64"}@`:`@esbuild+${process.platform}-${process.arch}@`,dir=readdirSync(path.join(root,"node_modules/.pnpm")).find(x=>x.startsWith(prefix));if(!dir)throw new Error("esbuild missing");const name=dir.slice(0,dir.lastIndexOf("@")).replace("+","/"),esbuild=path.join(root,"node_modules/.pnpm",dir,"node_modules",name,"bin/esbuild"),cache=path.join(root,"node_modules/.cache");mkdirSync(cache,{recursive:true});const temp=mkdtempSync(path.join(cache,"samurai-retention-")),out=path.join(temp,"verify.mjs"),sources=["packages/workspace-store/src/index.ts","scripts/fixtures/resource-retention.ts","scripts/verify-resource-retention.mjs","scripts/lib/core-evidence.mjs"];try{execFileSync(esbuild,[path.join(root,"scripts/fixtures/resource-retention.ts"),"--bundle","--platform=node","--format=esm","--external:better-sqlite3",`--outfile=${out}`],{cwd:root,stdio:"inherit"});const started_at=new Date().toISOString(),raw=execFileSync(process.execPath,[out],{cwd:root,encoding:"utf8"}).trim(),result=JSON.parse(raw),completed_at=new Date().toISOString(),evidence=path.join(root,"reports/core-completion/evidence");mkdirSync(evidence,{recursive:true});writeFileSync(path.join(evidence,"G02.json"),`${JSON.stringify({schema_version:1,test_id:"G02",command:"pnpm core:test:retention",status:"passed",...committedSourceEvidence(root,sources),started_at,completed_at,assertions:[{name:"Queue limit rejects overflow and trims completed delivery history",actual:result.queue_overflow_rejected&&result.queue_active===2,expected:true},{name:"Expired concurrency locks are reclaimed",actual:result.concurrency_reclaimed,expected:1},{name:"Context token and Workspace file limits are reported",actual:{tokens:result.context_token_limit,file_bytes:result.file_bytes},expected:{tokens:4000,file_bytes:"<=104857600"}},{name:"Event, backup, snapshot and index retention applies",actual:{events:result.event_retained,backups:result.backup_retained,snapshots:result.snapshot_retained,index:result.index_rebuilt},expected:{events:2,backups:2,snapshots:2,index:true}}],result},null,2)}\n`);process.stdout.write(`${raw}\n`)}finally{rmSync(temp,{recursive:true,force:true})}
