import{execFileSync}from"node:child_process";import{mkdirSync,mkdtempSync,readdirSync,rmSync,writeFileSync}from"node:fs";import path from"node:path";import process from"node:process";import{committedSourceEvidence}from"./lib/core-evidence.mjs";const root=process.cwd(),prefix=process.platform==="darwin"?`@esbuild+darwin-${process.arch==="arm64"?"arm64":"x64"}@`:`@esbuild+${process.platform}-${process.arch}@`,dir=readdirSync(path.join(root,"node_modules/.pnpm")).find(x=>x.startsWith(prefix));if(!dir)throw new Error("esbuild missing");const name=dir.slice(0,dir.lastIndexOf("@")).replace("+","/"),esbuild=path.join(root,"node_modules/.pnpm",dir,"node_modules",name,"bin/esbuild"),cache=path.join(root,"node_modules/.cache");mkdirSync(cache,{recursive:true});const temp=mkdtempSync(path.join(cache,"samurai-guardrails-")),out=path.join(temp,"verify.mjs"),sources=["packages/learning/src/guardrail-evaluation.ts","packages/workspace-store/src/index.ts","scripts/fixtures/learning-guardrails.ts","scripts/verify-learning-guardrails.mjs","scripts/lib/core-evidence.mjs"];try{execFileSync(esbuild,[path.join(root,"scripts/fixtures/learning-guardrails.ts"),"--bundle","--platform=node","--format=esm","--external:better-sqlite3",`--outfile=${out}`],{cwd:root,stdio:"inherit"});const started_at=new Date().toISOString(),raw=execFileSync(process.execPath,[out],{cwd:root,encoding:"utf8"}).trim(),result=JSON.parse(raw),completed_at=new Date().toISOString(),evidence=path.join(root,"reports/core-completion/evidence");mkdirSync(evidence,{recursive:true});writeFileSync(path.join(evidence,"E10.json"),`${JSON.stringify({schema_version:1,test_id:"E10",command:"pnpm core:test:learning-guardrails",status:"passed",...committedSourceEvidence(root,sources),started_at,completed_at,assertions:[{name:"Same-fingerprint work quality and Generated Surface outcome improve",actual:{quality_delta:result.quality_delta,ui_success_delta:result.ui_success_delta},expected:{quality_delta:">=5",ui_success_delta:">=0.1"}},{name:"Safety, correction, misapplication, cost and latency guardrails all pass",actual:Object.fromEntries(Object.entries(result.guardrails).map(([key,value])=>[key,value.passed])),expected:{safety:true,correction:true,misapplication:true,cost:true,latency:true}},{name:"Unsafe, expensive and incompatible candidates are rejected",actual:result.unsafe_rejected&&result.cost_regression_rejected&&result.incompatible_rejected,expected:true},{name:"Insufficient samples hold and successful Evaluation persists",actual:result.insufficient_held&&result.evaluation_persisted,expected:true}],result},null,2)}\n`);process.stdout.write(`${raw}\n`)}finally{rmSync(temp,{recursive:true,force:true})}
