import { execFileSync } from "node:child_process"; import { mkdirSync, mkdtempSync, readdirSync, rmSync, writeFileSync } from "node:fs"; import path from "node:path"; import process from "node:process"; import { committedSourceEvidence } from "./lib/core-evidence.mjs";
const root=process.cwd(), prefix=process.platform==="darwin"?`@esbuild+darwin-${process.arch==="arm64"?"arm64":"x64"}@`:`@esbuild+${process.platform}-${process.arch}@`; const dir=readdirSync(path.join(root,"node_modules/.pnpm")).find((entry)=>entry.startsWith(prefix)); if(!dir) throw new Error(`esbuild native package not found: ${prefix}`); const name=dir.slice(0,dir.lastIndexOf("@")).replace("+","/"), esbuild=path.join(root,"node_modules/.pnpm",dir,"node_modules",name,"bin/esbuild"), cache=path.join(root,"node_modules/.cache"); mkdirSync(cache,{recursive:true}); const temporary=mkdtempSync(path.join(cache,"samurai-user-model-")), output=path.join(temporary,"verify.mjs"); const sources=["packages/core-schemas/src/index.ts","packages/runtime/src/context/user-model.ts","scripts/fixtures/user-model.ts","scripts/verify-user-model.mjs","scripts/lib/core-evidence.mjs"];
try { execFileSync(esbuild,[path.join(root,"scripts/fixtures/user-model.ts"),"--bundle","--platform=node","--format=esm",`--outfile=${output}`],{cwd:root,stdio:"inherit"}); const started_at=new Date().toISOString(), raw=execFileSync(process.execPath,[output],{cwd:root,encoding:"utf8"}).trim(), result=JSON.parse(raw), completed_at=new Date().toISOString(), evidenceDir=path.join(root,"reports/core-completion/evidence"); mkdirSync(evidenceDir,{recursive:true}); writeFileSync(path.join(evidenceDir,"E01.json"),`${JSON.stringify({schema_version:1,test_id:"E01",command:"pnpm core:test:user-model",status:"passed",...committedSourceEvidence(root,sources),started_at,completed_at,assertions:[{name:"Fact count and prompt size are bounded",actual:result.fact_count<=result.max_facts&&result.prompt_chars<=result.prompt_max_chars,expected:true},{name:"Every fact preserves source references",actual:result.all_source_backed,expected:true},{name:"Same-key update replaces and versions",actual:result.update_replaced_key,expected:true},{name:"Fact deletion works",actual:result.deletion_applied,expected:true},{name:"Long raw history is not injected",actual:result.raw_history_injected,expected:false}],result},null,2)}\n`); process.stdout.write(`${raw}\n`); } finally { rmSync(temporary,{recursive:true,force:true}); }
