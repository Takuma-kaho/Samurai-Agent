# 2026-07-02 作業メモ

## 目的

- Samurai Agent の現在地を、次の作業に入りやすい形で短く残す。
- 既存の正本ドキュメントとズレないように、判断の前提を固定する。

## 確認した正本

- `ARCHITECTURE.md`
- `PRINCIPLES.md`
- `PUBLIC_NAMING.md`
- `WEB_UI_DESIGN.md`

## 現在の前提

- Samurai Agent の主語は、単なるチャットAIではなく `GUI-first Personal Agent Workspace`。
- Chat は入口で、Workspace が本体。
- Agent Backend は固定せず、cassette として差し替えられる構造にする。
- Memory / Skill / Artifact / Collection は Workspace 側に残す。
- 公開面では、参照元プロジェクト名を出しすぎない。
- Web UI は Dark-only、Chat-first、Workspace on demand、Low text を基本にする。

## 現在の作業状態

- `git status` 上、既存の未コミット変更が複数ある。
- そのため、このメモでは既存ファイルの整理や差し戻しはしない。
- 次の実装に入る前に、未コミット変更の意図を確認した方が安全。

## 次に見るとよい場所

- UIを触る場合: `apps/web/src/App.vue`, `apps/web/src/styles/app.css`, `apps/web/src/lib/api.ts`
- Backend境界を見る場合: `packages/agent-backends/src/index.ts`, `packages/runtime/src/index.ts`
- release readiness を見る場合: `plans/backend-architecture-completion-ledger.md`
- UI方針を確認する場合: `WEB_UI_DESIGN.md`

## 次の作業候補

- 未コミット変更を分類する。
- UI変更、backend変更、設計資料変更を分けて差分確認する。
- 目的が決まったら、該当領域だけ最小修正する。
- 実装後は、対象に合わせて lint / typecheck / test を実行する。
