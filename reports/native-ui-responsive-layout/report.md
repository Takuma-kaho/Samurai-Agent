# Native UI レスポンシブ調整 検証報告

実施日: 2026-09-20

## 実装範囲

- 左サイドバーと右パネルを常時リサイズ可能に変更。
- 左220px、チャット280px、右パネル260pxを最小幅とし、ドラッグ・キーボードで最小幅未満を指定すると閉じる。
- 右パネルをオーバーレイ化せず、チャットを狭める通常の2カラム表示に統一。
- チャット本文を`max-width: 800px`のレスポンシブ領域へ変更し、スクロールバーを細く右端へ配置。
- プロフィール下の「設定」と上矢印を削除。
- 参加者表示を人型アイコン＋人数へ変更。

## 自動検証

- 対象Vitest: 5ファイル / 43テスト、全件成功。
- Web typecheck: 成功。
- `pnpm lint`: 成功。
- Web build: 成功。既存の大きなチャンク警告のみ。
- `git diff --check`: 成功。
- ルート`pnpm test`: 224ファイル中、既存のRoomナビゲーションテスト2件が失敗。今回変更したファイル外の`native-room-mark`／`--native-room-depth`期待値と現行実装の不一致で、今回の変更による失敗ではない。

## 実画面確認

既存の検証プロセス（launcher PID 6664、Vite PID 6665、Electron PID 6669）を再利用し、再起動・停止はしていない。サイドバーの幅変更ハンドルとプロフィール表示から「設定」サブテキスト・上矢印が消えたことは確認した。

その後のHMR再読込では、既存Serverへの`127.0.0.1:4337`接続が`ECONNREFUSED`となり白画面になったため、右パネル・参加者表示・スクロールバーの実画面確認は未確認。自動検証結果を完成証拠と混同しない。
