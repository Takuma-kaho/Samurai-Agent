# AGENTS.md

- 日本語で簡潔に答え、確認した事実と推測を分ける。
- 私を「ドパガキくん」と呼ぶこと。
- 設計・実装・UI・命名の変更前に、`PRODUCT.md`、`ARCHITECTURE.md`、作業対象に最も近い`AGENTS.md`、関係する設計書を読む。
- 既存差分と依頼範囲を守り、secretを扱わず、依頼なしにbranch作成・commit・pushを行わない。
- 文書は用語・リンク・Mermaid・`git diff --check`、コードは対象のlint・typecheck・testを確認し、未検証範囲を明記する。
- UIデザインを計画、実装する時、https://github.com/bergside/awesome-design-skills.gitのスキルを利用する。

## フォルダ案内

- `plans/`：合意した実装計画と作業手順を置く。
- `reports/`：実際に行った検証の条件、結果、未検証範囲を置く。
- `docs/designs/`：現在の仕組みを説明する設計書を置く。作成・更新は利用者の明示指示がある場合だけ行う。
- `docs/designs/README.md`：個別設計書の現在一覧と作成候補を確認する目次。
- `README.md`：人が最初に読むプロジェクト案内を置く。
- 作業対象に最も近い`AGENTS.md`がある場合、そのフォルダ固有の配置・命名規則に従う。

## 配置・保存

- ファイル名だけで対象、機能、目的が分かる名前にする。
- 新しいファイルは責務を持つ既存フォルダへ置き、ルートへ独立文書やフォルダを増やさない。
- 「プランを作って」と依頼された場合、チャットだけの指定がなければ完成した計画を`plans/`へ保存する。
- 実装や検証を実際に行った場合は、再利用できる結果を`reports/`へ記録する。説明・相談・計画だけでは作成しない。
- `plans/`、`reports/`、`docs/designs/`の保存先や作成有無は、その場の利用者の指示を最優先する。

## 実装・修正・レビューの共通原則

以下は初回実装と、レビュー・検証で問題が見つかった後の修正に適用する。

### 1. 製品の本質を守る

- 正本と合意済みの目的・対象範囲・対象外・完了条件を維持する。
  各プランには、今回守る製品価値と、禁止する回避策を具体的に記す。
- 修正前に発生条件・原因・影響を確認し、製品価値と責務境界を保ちながら
  原因を解消できる最も単純な方法を選ぶ。
- 問題回避を目的とした、無断の機能削減・利用制限・承認や手作業の追加、
  根拠のない防御処理・汎用化を行わない。
- 成功扱いにするための要件削減、テストの弱体化、必要な検証の省略、
  エラー隠蔽、仮実装の完成扱いを禁止する。

### 2. 業務ルールとデータ整合性を共通Coreで守る

- 業務ルール・権限・データ整合性を、呼び出し側の正しい実装に依存させない。
  呼び出し側に検証漏れや誤った入力・操作順序があっても、共通Core自身が
  必要な検証を行い、不正な変更を拒否して整合性を保つ。
- Controller・UI・API handlerは入力と出力の処理、共通処理の呼び出しを
  中心とし、業務判断・認可・整合性の唯一の実装場所にしない。
  入口で必要な認証・入力形式の検査も省略しない。
- 機能や入口を追加する場合も、共通処理を迂回して業務データを更新しない。
  必要なDB制約・トランザクションを用い、対象に応じて不正な呼び出し、
  部分失敗、競合時にも守るべき条件が維持されることを検証する。
- 第三者が責務・依存関係・処理の流れ・失敗時の動作を追える構造にする。
  巨大な処理を別の巨大なModel・Serviceへ移すだけの修正や、
  不要な細分化・抽象化を避け、理解・再利用・変更のしやすさを保つ。

### 3. 根拠と完了条件で修正範囲を決める

- 指摘は「今回の修正必須」「追加調査」「任意改善」「対象外」に分類する。
  修正必須とする場合は、違反する要件や守るべき条件、発生条件、
  コード上の根拠、具体的な影響を示し、既存の仕組みで防げていないか確認する。
- 未検証を成功や不具合と断定しない。重大な権限逸脱やデータ破損の疑いは、
  優先して根拠を調べる。指摘されたことだけを理由に修正を実装しない。
- 再レビューは修正箇所と影響範囲を中心に行い、前回の判断と根拠を引き継ぐ。
  新しい根拠なしに解決済みの指摘を蒸し返したり、任意改善を完了条件へ
  追加したりしない。根拠のある指摘が0件でも有効な結果とする。
- 合意要件を満たし、必要な検証に合格し、修正必須事項と完了に必要な
  未確認事項が解消した時点で完了とする。新たな変更・失敗・根拠がなければ、
  検証や問題探しを際限なく繰り返さない。

### 4. 承認済みの作業を最後まで進める

- 実装を依頼された範囲では、実装・必要な修正・検証まで継続する。
  通常の実装判断は既存方針に沿って行い、同じ承認の取り直しや
  工程ごとの不要な続行確認を行わない。
- 工程の区切りや進捗報告を作業終了の理由にしない。
  合意範囲の未完了項目があり、実行可能なら次の工程へ進む。
- 明示された停止地点、未承認の操作、利用者の判断や権限変更が不可欠な
  障害がある場合は、該当作業を保留する。独立して進められる作業は続行する。
  確認時は具体的な障害と必要な判断を示し、指示が理由なら出典と該当文を示す。
- 長時間作業では、実装状況・検証結果・残作業・承認済み範囲・次の工程を
  既存のプランや作業記録へ残す。再開時は記録と現状を照合して継続する。
  一部工程の完了を全体の完了とせず、中断時は未完了事項と再開条件を明記する。
