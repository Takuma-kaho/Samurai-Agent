# AGENTS.md

- 日本語で簡潔に答え、確認した事実と推測を分ける。
- 私を「ドパガキくん」と呼ぶこと。
- 設計・実装・UI・命名の変更前に、`PRODUCT.md`、`ARCHITECTURE.md`、作業対象に最も近い`AGENTS.md`、関係する設計書を読む。
- 既存差分と依頼範囲を守り、secretを扱わず、依頼なしにbranch作成・commit・pushを行わない。
- 文書は用語・リンク・Mermaid・`git diff --check`、コードは対象のlint・typecheck・testを確認し、未検証範囲を明記する。

## フォルダ案内

- `plans/`：合意した実装計画と作業手順を置く。
- `reports/`：実際に行った検証の条件、結果、未検証範囲を置く。
- `docs/designs/`：現在の仕組みを説明する設計書を置く。作成・更新は利用者の明示指示がある場合だけ行う。
- `docs/designs/README.md`：個別設計書の現在一覧と作成候補を確認する目次。
- `README.md`：人が最初に読むプロジェクト案内を置く。
- 作業対象に最も近い`AGENTS.md`がある場合、そのフォルダ固有の配置・命名規則に従う。

## 配置・保存

- ファイル名だけで対象、機能、目的が分かる名前にする。
- 新しいファイルは責務を持つ既存フォルダへ置き、ルートへ独立文書やフォルダを増やさない。
- 「プランを作って」と依頼された場合、チャットだけの指定がなければ完成した計画を`plans/`へ保存する。
- 実装や検証を実際に行った場合は、再利用できる結果を`reports/`へ記録する。説明・相談・計画だけでは作成しない。
- `plans/`、`reports/`、`docs/designs/`の保存先や作成有無は、その場の利用者の指示を最優先する。
